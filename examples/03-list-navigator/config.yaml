app-name: "my-app"
debug: false
server:
  host: localhost
  port: 8080
  pool:
    min-size: 2
    max-size: 10
workers:
  - name: "default"
    concurrency: 2
  - name: "background"
    concurrency: 1
